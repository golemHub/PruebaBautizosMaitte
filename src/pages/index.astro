---
import Layout from "../layouts/Layout.astro"
---

<Layout>
<!-- Hero Section -->
  <section class="hero">
    <span class="hero-badge">Colección Bautizo 2026</span>
    <h1>El día más <em>especial</em> merece lo mejor</h1>
    <p>Descubre nuestra exclusiva colección de trajes, vestidos y artículos de souvenir para crear recuerdos inolvidables en el bautizo de tu pequeño.</p>
    <div class="hero-buttons">
      <a href="/productos" class="btn btn-primary">Ver Colección</a>
      <a href="#map-section" class="btn btn-secondary">Visítanos</a>
    </div>
    <div class="hero-scroll">
      <span>Descubre más</span>
      <div class="scroll-line"></div>
    </div>
  </section>

  <!-- Categories Strip -->
  <section class="categories-strip">
    <div class="categories-strip-inner">
      <a href="#" class="category-link">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
          <path d="M20.38 3.46 16 2a4 4 0 0 1-8 0L3.62 3.46a2 2 0 0 0-1.34 2.23l.58 3.47a1 1 0 0 0 .99.84H6v10c0 1.1.9 2 2 2h8a2 2 0 0 0 2-2V10h2.15a1 1 0 0 0 .99-.84l.58-3.47a2 2 0 0 0-1.34-2.23z"/>
        </svg>
        Trajes Niño
      </a>
      <a href="#" class="category-link">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
          <path d="M6 2v1a2 2 0 0 0 2 2h8a2 2 0 0 0 2-2V2"/>
          <path d="M4 6h16l-1.5 9.5a2 2 0 0 1-2 1.5H7.5a2 2 0 0 1-2-1.5L4 6z"/>
          <path d="M8 22v-4h8v4"/>
        </svg>
        Vestidos Niña
      </a>
      <a href="#" class="category-link">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
          <path d="M2 18a1 1 0 0 0 1 1h18a1 1 0 0 0 1-1v-2a1 1 0 0 0-1-1H3a1 1 0 0 0-1 1v2z"/>
          <path d="M10 10V5a1 1 0 0 1 1-1h2a1 1 0 0 1 1 1v5"/>
          <path d="M4 15v-3a6 6 0 0 1 6-6h4a6 6 0 0 1 6 6v3"/>
        </svg>
        Zapatos
      </a>
      <a href="#" class="category-link">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
          <path d="M21 8a2 2 0 0 0-1-1.73l-7-4a2 2 0 0 0-2 0l-7 4A2 2 0 0 0 3 8v8a2 2 0 0 0 1 1.73l7 4a2 2 0 0 0 2 0l7-4A2 2 0 0 0 21 16Z"/>
          <path d="m3.3 7 8.7 5 8.7-5"/>
          <path d="M12 22V12"/>
        </svg>
        Souvenirs
      </a>
      <a href="#" class="category-link">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
          <path d="M12 3a6 6 0 0 0 9 9 9 9 0 1 1-9-9Z"/>
        </svg>
        Accesorios
      </a>
    </div>
  </section>

  <!-- Featured Product -->
  <section class="featured-product">
    <div class="featured-product-inner">
      <div class="featured-image">
        <img src="/placeholder.svg?height=600&width=600" alt="Vestido de Bautizo Premium">
        <span class="featured-badge">Más Vendido</span>
      </div>
      <div class="featured-content">
        <div class="section-title" style="text-align: left; margin-bottom: 1.5rem;">
          <span>Producto Estrella</span>
        </div>
        <h3>Vestido Celestial de Seda y Encaje</h3>
        <p class="price">€189.00</p>
        <p>Nuestro vestido insignia, confeccionado a mano con los más finos materiales. Seda italiana de primera calidad combinada con delicados encajes franceses, creando una pieza única que será el centro de todas las miradas.</p>
        <div class="featured-details">
          <div class="featured-detail">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
              <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10"/>
            </svg>
            100% Materiales premium certificados
          </div>
          <div class="featured-detail">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
              <path d="M14.5 4h-5L7 7H4a2 2 0 0 0-2 2v9a2 2 0 0 0 2 2h16a2 2 0 0 0 2-2V9a2 2 0 0 0-2-2h-3l-2.5-3z"/>
              <circle cx="12" cy="13" r="3"/>
            </svg>
            Personalizable con bordados
          </div>
          <div class="featured-detail">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
              <rect x="1" y="3" width="15" height="13" rx="2"/>
              <path d="m16 8 5 3-5 3V8z"/>
            </svg>
            Incluye caja de regalo premium
          </div>
        </div>
        <a href="#" class="btn btn-primary">Añadir al Carrito</a>
      </div>
    </div>
  </section>

  <!-- Bento Categories -->
  <section class="bento-section">
    <div class="section-title">
      <span>Explora</span>
      <h2>Nuestras Categorías</h2>
    </div>
    <div class="bento-grid">
      <div class="bento-item">
        <img src="/trajes-niño.jpg?height=600&width=800" alt="Trajes de Niño">
        <div class="bento-overlay">
          <span>Colección Completa</span>
          <h3>Trajes de Niño</h3>
        </div>
      </div>
      <div class="bento-item">
        <img src="/vestidos-niña.jpg?height=300&width=400" alt="Vestidos de Niña">
        <div class="bento-overlay">
          <span>12 Productos</span>
          <h3>Vestidos Niña</h3>
        </div>
      </div>
      <div class="bento-item">
        <img src="/zapatos.jpg?height=300&width=400" alt="Zapatos">
        <div class="bento-overlay">
          <span>8 Productos</span>
          <h3>Zapatos</h3>
        </div>
      </div>
      <div class="bento-item">
        <img src="/souvenirs.jpg?height=300&width=400" alt="Souvenirs">
        <div class="bento-overlay">
          <span>25 Productos</span>
          <h3>Souvenirs</h3>
        </div>
      </div>
      <div class="bento-item">
        <img src="/accesorios.jpg?height=300&width=400" alt="Accesorios">
        <div class="bento-overlay">
          <span>15 Productos</span>
          <h3>Accesorios</h3>
        </div>
      </div>
    </div>
  </section>

  <!-- Products Grid -->
  <section class="products-section">
    <div class="section-title">
      <span>Nuevas Llegadas</span>
      <h2>Productos Destacados</h2>
    </div>
    <div class="products-grid">
      <div class="product-card">
        <div class="product-image">
          <img src="/placeholder.svg?height=350&width=300" alt="Traje Romeo">
          <div class="product-actions">
            <button class="product-action-btn">Ver Detalles</button>
            <button class="product-action-btn">Añadir</button>
          </div>
        </div>
        <div class="product-info">
          <p class="product-category">Trajes Niño</p>
          <h4 class="product-name">Traje Romeo</h4>
          <p class="product-price">€129.00</p>
        </div>
      </div>
      <div class="product-card">
        <div class="product-image">
          <img src="/placeholder.svg?height=350&width=300" alt="Vestido Angelina">
          <div class="product-actions">
            <button class="product-action-btn">Ver Detalles</button>
            <button class="product-action-btn">Añadir</button>
          </div>
        </div>
        <div class="product-info">
          <p class="product-category">Vestidos Niña</p>
          <h4 class="product-name">Vestido Angelina</h4>
          <p class="product-price"><span class="old-price">€159.00</span> €135.00</p>
        </div>
      </div>
      <div class="product-card">
        <div class="product-image">
          <img src="/placeholder.svg?height=350&width=300" alt="Zapatos Primeros Pasos">
          <div class="product-actions">
            <button class="product-action-btn">Ver Detalles</button>
            <button class="product-action-btn">Añadir</button>
          </div>
        </div>
        <div class="product-info">
          <p class="product-category">Zapatos</p>
          <h4 class="product-name">Primeros Pasos</h4>
          <p class="product-price">€49.00</p>
        </div>
      </div>
      <div class="product-card">
        <div class="product-image">
          <img src="/placeholder.svg?height=350&width=300" alt="Set Recuerdo Premium">
          <div class="product-actions">
            <button class="product-action-btn">Ver Detalles</button>
            <button class="product-action-btn">Añadir</button>
          </div>
        </div>
        <div class="product-info">
          <p class="product-category">Souvenirs</p>
          <h4 class="product-name">Set Recuerdo Premium</h4>
          <p class="product-price">€75.00</p>
        </div>
      </div>
    </div>
  </section>

  <!-- Testimonial -->
  <section class="testimonial-section">
    <div class="testimonial-inner">
      <div class="section-title">
        <span>Testimonios</span>
      </div>
      <div class="testimonial-stars">
        <svg viewBox="0 0 24 24"><path d="m12 2 3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z"/></svg>
        <svg viewBox="0 0 24 24"><path d="m12 2 3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z"/></svg>
        <svg viewBox="0 0 24 24"><path d="m12 2 3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z"/></svg>
        <svg viewBox="0 0 24 24"><path d="m12 2 3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z"/></svg>
        <svg viewBox="0 0 24 24"><path d="m12 2 3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z"/></svg>
      </div>
      <p class="testimonial-quote">"El vestido de mi pequeña fue absolutamente perfecto. La calidad superó todas mis expectativas y el servicio fue excepcional. Todos los invitados quedaron maravillados."</p>
      <p class="testimonial-author">
        <strong>María García</strong>
        Bautizo de Sofía, Madrid
      </p>
    </div>
  </section>

  <!-- Newsletter -->
  <section class="newsletter-section">
    <div class="newsletter-inner">
      <h2>Únete a Nuestra Familia</h2>
      <p>Suscríbete para recibir ofertas exclusivas, novedades y un 10% de descuento en tu primera compra.</p>
      <form class="newsletter-form">
        <input type="email" placeholder="Tu correo electrónico">
        <button type="submit">Suscribirse</button>
      </form>
    </div>
  </section>

  <!-- map -->
<section class="map-section" id="map-section">
    <div class="map-container">
      <div class="map-info">
        <div class="section-title" style="text-align: left; margin-bottom: 1.5rem;">
          <span>Visítanos</span>
        </div>
        <div class="info-item">
          <h4>Ubicación</h4>
          <p>Benavente 440<br>Ovalle, Chile</p>
        </div>

        <div class="info-item">
          <h4>Horario de Atención</h4>
          <p>Lunes a Sábado: 10:00 - 20:00</p>
        </div>

        <div class="info-item">
          <h4>Contacto Directo</h4>
          <p>Tel: +56 9 5396 2462</p>
        </div>
        
        <a href="https://maps.google.com/?q=Benavente+440,+Ovalle,+Chile" target="_blank" class="btn btn-secondary" style="margin-top: 1rem;">
          Cómo llegar
        </a>
      </div>

      <div class="map-frame">
        <iframe 
          src="https://www.google.com/maps/embed?pb=!1m18!1m12!1m3!1d3455.572111585!2d-71.2033!3d-30.5986!2m3!1f0!2f0!3f0!3m2!1i1024!2i768!4f13.1!3m3!1m2!1s0x9689408f657e750f%3A0x6b8f1c3f8f1c3f8f!2sBenavente%20440%2C%20Ovalle%2C%20Coquimbo%2C%20Chile!5e0!3m2!1ses!2scl!4v1700000000000" 
          style="border:0;" 
          allowfullscreen="" 
          loading="lazy" 
          referrerpolicy="no-referrer-when-downgrade">
        </iframe>
      </div>
    </div>
  </section>
</Layout>

<style>

/* Hero Section */
    .hero {
      padding: 10rem 2rem 6rem;
      text-align: center;
      background: linear-gradient(to bottom, var(--color-cream) 0%, var(--color-white) 100%);
      min-height: 100vh;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
    }

    .hero-badge {
      display: inline-flex;
      align-items: center;
      gap: 0.5rem;
      background: var(--color-white);
      border: 1px solid var(--color-gold-light);
      padding: 0.5rem 1.25rem;
      border-radius: 2rem;
      font-size: 0.75rem;
      font-weight: 500;
      color: var(--color-gray);
      text-transform: uppercase;
      letter-spacing: 0.1em;
      margin-bottom: 2rem;
    }

    .hero h1 {
      font-family: var(--font-heading);
      font-size: clamp(3rem, 8vw, 6rem);
      font-weight: 400;
      line-height: 1.1;
      margin-bottom: 1.5rem;
      max-width: 900px;
    }

    .hero h1 em {
      font-style: italic;
      color: var(--color-gold);
    }

    .hero p {
      font-size: 1.1rem;
      color: var(--color-gray);
      max-width: 600px;
      margin: 0 auto 3rem;
      font-weight: 300;
    }

    .hero-buttons {
      display: flex;
      gap: 1rem;
      flex-wrap: wrap;
      justify-content: center;
    }

    .btn {
      display: inline-flex;
      align-items: center;
      gap: 0.5rem;
      padding: 1rem 2.5rem;
      font-size: 0.8rem;
      font-weight: 500;
      text-transform: uppercase;
      letter-spacing: 0.15em;
      text-decoration: none;
      border-radius: 0;
      transition: all 0.3s ease;
      cursor: pointer;
      border: none;
    }

    .btn-primary {
      background: var(--color-charcoal);
      color: var(--color-white);
    }

    .btn-primary:hover {
      background: var(--color-gold);
    }

    .btn-secondary {
      background: transparent;
      color: var(--color-charcoal);
      border: 1px solid var(--color-charcoal);
    }

    .btn-secondary:hover {
      background: var(--color-charcoal);
      color: var(--color-white);
    }

    .hero-scroll {
      margin-top: 4rem;
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: 0.5rem;
      color: var(--color-gray-light);
      font-size: 0.7rem;
      text-transform: uppercase;
      letter-spacing: 0.15em;
    }

    .scroll-line {
      width: 1px;
      height: 60px;
      background: linear-gradient(to bottom, var(--color-gold), transparent);
    }
/* Categories Strip */
    .categories-strip {
      background: var(--color-white);
      padding: 3rem 2rem;
      border-top: 1px solid var(--color-gold-light);
      border-bottom: 1px solid var(--color-gold-light);
    }

    .categories-strip-inner {
      max-width: 1400px;
      margin: 0 auto;
      display: flex;
      justify-content: center;
      align-items: center;
      gap: 4rem;
      flex-wrap: wrap;
    }

    .category-link {
      display: flex;
      align-items: center;
      gap: 0.75rem;
      text-decoration: none;
      color: var(--color-charcoal);
      font-size: 0.85rem;
      font-weight: 500;
      text-transform: uppercase;
      letter-spacing: 0.1em;
      transition: color 0.3s ease;
    }

    .category-link:hover {
      color: var(--color-gold);
    }

    .category-link svg {
      width: 24px;
      height: 24px;
      stroke: var(--color-gold);
    }

    /* Section Title */
    .section-title {
      text-align: center;
      margin-bottom: 4rem;
    }

    .section-title span {
      display: block;
      font-size: 0.75rem;
      font-weight: 500;
      text-transform: uppercase;
      letter-spacing: 0.2em;
      color: var(--color-gold);
      margin-bottom: 1rem;
    }

    .section-title h2 {
      font-family: var(--font-heading);
      font-size: clamp(2rem, 4vw, 3rem);
      font-weight: 400;
    }

    /* Featured Product */
    .featured-product {
      padding: 6rem 2rem;
      background: var(--color-white);
    }

    .featured-product-inner {
      max-width: 1400px;
      margin: 0 auto;
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 4rem;
      align-items: center;
    }

    .featured-image {
      position: relative;
    }

    .featured-image img {
      width: 100%;
      height: 600px;
      object-fit: cover;
    }

    .featured-badge {
      position: absolute;
      top: 2rem;
      left: 2rem;
      background: var(--color-gold);
      color: var(--color-white);
      padding: 0.5rem 1.25rem;
      font-size: 0.7rem;
      font-weight: 600;
      text-transform: uppercase;
      letter-spacing: 0.1em;
    }

    .featured-content h3 {
      font-family: var(--font-heading);
      font-size: 2.5rem;
      font-weight: 400;
      margin-bottom: 1rem;
    }

    .featured-content .price {
      font-family: var(--font-heading);
      font-size: 1.75rem;
      color: var(--color-gold);
      margin-bottom: 1.5rem;
    }

    .featured-content p {
      color: var(--color-gray);
      margin-bottom: 2rem;
      font-weight: 300;
      line-height: 1.8;
    }

    .featured-details {
      display: flex;
      flex-direction: column;
      gap: 1rem;
      margin-bottom: 2.5rem;
      padding: 1.5rem 0;
      border-top: 1px solid var(--color-gold-light);
      border-bottom: 1px solid var(--color-gold-light);
    }

    .featured-detail {
      display: flex;
      align-items: center;
      gap: 1rem;
      font-size: 0.85rem;
      color: var(--color-gray);
    }

    .featured-detail svg {
      width: 20px;
      height: 20px;
      stroke: var(--color-gold);
    }

    /* Bento Grid */
    .bento-section {
      padding: 6rem 2rem;
      background: var(--color-cream);
    }

    .bento-grid {
      max-width: 1400px;
      margin: 0 auto;
      display: grid;
      grid-template-columns: repeat(4, 1fr);
      grid-template-rows: repeat(2, 300px);
      gap: 1.5rem;
    }

    .bento-item {
      position: relative;
      overflow: hidden;
      cursor: pointer;
    }

    .bento-item img {
      width: 100%;
      height: 100%;
      object-fit: cover;
      transition: transform 0.6s ease;
    }

    .bento-item:hover img {
      transform: scale(1.05);
    }

    .bento-overlay {
      position: absolute;
      inset: 0;
      background: linear-gradient(to top, rgba(44, 44, 44, 0.8) 0%, transparent 60%);
      display: flex;
      flex-direction: column;
      justify-content: flex-end;
      padding: 2rem;
      transition: background 0.3s ease;
    }

    .bento-item:hover .bento-overlay {
      background: linear-gradient(to top, rgba(201, 169, 98, 0.9) 0%, rgba(201, 169, 98, 0.3) 100%);
    }

    .bento-overlay span {
      font-size: 0.7rem;
      text-transform: uppercase;
      letter-spacing: 0.15em;
      color: var(--color-gold-light);
      margin-bottom: 0.5rem;
    }

    .bento-overlay h3 {
      font-family: var(--font-heading);
      font-size: 1.5rem;
      color: var(--color-white);
      font-weight: 400;
    }

    .bento-item:nth-child(1) {
      grid-column: span 2;
      grid-row: span 2;
    }

    .bento-item:nth-child(1) .bento-overlay h3 {
      font-size: 2.5rem;
    }

    /* Products Grid */
    .products-section {
      padding: 6rem 2rem;
      background: var(--color-white);
    }

    .products-grid {
      max-width: 1400px;
      margin: 0 auto;
      display: grid;
      grid-template-columns: repeat(4, 1fr);
      gap: 2rem;
    }

    .product-card {
      cursor: pointer;
    }

    .product-image {
      position: relative;
      overflow: hidden;
      margin-bottom: 1.5rem;
      background: var(--color-cream);
    }

    .product-image img {
      width: 100%;
      height: 350px;
      object-fit: cover;
      transition: transform 0.5s ease;
    }

    .product-card:hover .product-image img {
      transform: scale(1.03);
    }

    .product-actions {
      position: absolute;
      bottom: 1rem;
      left: 1rem;
      right: 1rem;
      display: flex;
      gap: 0.5rem;
      opacity: 0;
      transform: translateY(10px);
      transition: all 0.3s ease;
    }

    .product-card:hover .product-actions {
      opacity: 1;
      transform: translateY(0);
    }

    .product-action-btn {
      flex: 1;
      background: var(--color-white);
      border: none;
      padding: 0.75rem;
      font-size: 0.7rem;
      font-weight: 600;
      text-transform: uppercase;
      letter-spacing: 0.1em;
      cursor: pointer;
      transition: all 0.3s ease;
    }

    .product-action-btn:hover {
      background: var(--color-charcoal);
      color: var(--color-white);
    }

    .product-info {
      text-align: center;
    }

    .product-category {
      font-size: 0.7rem;
      text-transform: uppercase;
      letter-spacing: 0.15em;
      color: var(--color-gold);
      margin-bottom: 0.5rem;
    }

    .product-name {
      font-family: var(--font-heading);
      font-size: 1.25rem;
      color: var(--color-charcoal);
      margin-bottom: 0.5rem;
    }

    .product-price {
      font-size: 0.95rem;
      color: var(--color-gray);
    }

    .product-price .old-price {
      text-decoration: line-through;
      color: var(--color-gray-light);
      margin-right: 0.5rem;
    }

    /* Testimonial */
    .testimonial-section {
      padding: 6rem 2rem;
      background: var(--color-cream);
      text-align: center;
    }

    .testimonial-inner {
      max-width: 800px;
      margin: 0 auto;
    }

    .testimonial-quote {
      font-family: var(--font-heading);
      font-size: clamp(1.5rem, 3vw, 2rem);
      font-style: italic;
      font-weight: 400;
      line-height: 1.6;
      color: var(--color-charcoal);
      margin-bottom: 2rem;
    }

    .testimonial-author {
      font-size: 0.85rem;
      color: var(--color-gray);
    }

    .testimonial-author strong {
      display: block;
      color: var(--color-charcoal);
      font-weight: 600;
      margin-bottom: 0.25rem;
    }

    .testimonial-stars {
      display: flex;
      justify-content: center;
      gap: 0.25rem;
      margin-bottom: 1.5rem;
    }

    .testimonial-stars svg {
      width: 20px;
      height: 20px;
      fill: var(--color-gold);
    }

    /* Newsletter */
    .newsletter-section {
      padding: 6rem 2rem;
      background: var(--color-charcoal);
      text-align: center;
    }

    .newsletter-inner {
      max-width: 600px;
      margin: 0 auto;
    }

    .newsletter-section h2 {
      font-family: var(--font-heading);
      font-size: 2.5rem;
      font-weight: 400;
      color: var(--color-white);
      margin-bottom: 1rem;
    }

    .newsletter-section p {
      color: var(--color-gray-light);
      margin-bottom: 2rem;
      font-weight: 300;
    }

    .newsletter-form {
      display: flex;
      gap: 0;
      max-width: 500px;
      margin: 0 auto;
    }

    .newsletter-form input {
      flex: 1;
      padding: 1rem 1.5rem;
      font-size: 0.9rem;
      border: none;
      background: var(--color-white);
      font-family: var(--font-body);
    }

    .newsletter-form input::placeholder {
      color: var(--color-gray-light);
    }

    .newsletter-form button {
      padding: 1rem 2rem;
      background: var(--color-gold);
      color: var(--color-white);
      border: none;
      font-size: 0.8rem;
      font-weight: 600;
      text-transform: uppercase;
      letter-spacing: 0.1em;
      cursor: pointer;
      transition: background 0.3s ease;
      font-family: var(--font-body);
    }

    .newsletter-form button:hover {
      background: #B8983F;
}

/* Map Section */
    .map-section {
      padding: 6rem 2rem;
      background: var(--color-white);
    }

    .map-container {
      max-width: 1400px;
      margin: 0 auto;
      display: grid;
      grid-template-columns: 1fr 2fr;
      gap: 4rem;
      align-items: center;
    }

    .map-info h2 {
      font-family: var(--font-heading);
      font-size: 2.5rem;
      font-weight: 400;
      margin-bottom: 2rem;
    }

    .info-item {
      margin-bottom: 2rem;
    }

    .info-item h4 {
      font-size: 0.75rem;
      text-transform: uppercase;
      letter-spacing: 0.15em;
      color: var(--color-gold);
      margin-bottom: 0.5rem;
    }

    .info-item p {
      font-size: 1rem;
      color: var(--color-charcoal);
      font-weight: 400;
    }

    .map-frame {
      line-height: 0;
      border: 1px solid var(--color-gold-light);
      padding: 10px;
      background: var(--color-cream);
    }

    .map-frame iframe {
      width: 100%;
      height: 450px;
      filter: grayscale(0.2) contrast(0.9); /* Opcional: para un look más artístico */
    }

    /* Responsive */
    @media (max-width: 1024px) {
      .bento-grid {
        grid-template-columns: repeat(2, 1fr);
        grid-template-rows: repeat(3, 250px);
      }

      .bento-item:nth-child(1) {
        grid-column: span 2;
        grid-row: span 1;
      }

      .products-grid {
        grid-template-columns: repeat(2, 1fr);
      }

      .featured-product-inner {
        grid-template-columns: 1fr;
      }

      .map-container {
        grid-template-columns: 1fr;
        gap: 3rem;
      }
      
      .map-info {
        text-align: center;
      }

      .footer-inner {
        grid-template-columns: repeat(2, 1fr);
      }
    }
    @media (max-width: 768px) {
      nav {
        display: none;
      }

      .hero h1 {
        font-size: 2.5rem;
      }

      .categories-strip-inner {
        gap: 2rem;
      }

      .bento-grid {
        grid-template-columns: 1fr;
        grid-template-rows: auto;
      }

      .bento-item:nth-child(1) {
        grid-column: span 1;
      }

      .bento-item {
        height: 250px;
      }

      .products-grid {
        grid-template-columns: 1fr;
      }

      .newsletter-form {
        flex-direction: column;
      }

      .footer-inner {
        grid-template-columns: 1fr;
        gap: 2rem;
      }

      .footer-bottom {
        flex-direction: column;
        text-align: center;
      }
    }
</style>
